# Project-wide Gradle settings.

# Reduced memory settings to avoid "paging file is too small" errors.
org.gradle.jvmargs=-Xmx512m -XX:+UseG1GC

# AndroidX package structure
android.useAndroidX=true

# Valid Java Home pointing to a compatible JDK (17)
org.gradle.java.home=C\:/Users/SIVA/.gradle/jdks/eclipse_adoptium-17-amd64-windows.2
